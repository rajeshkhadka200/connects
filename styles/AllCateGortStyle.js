import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    paddingHorizontal: 10,
  },
  header: {
    backgroundColor: "#fff",
    borderRadius: 10,
    height: 60,
    flexDirection: "row",
    alignItems: "center",
    padding: 10,
    borderWidth: 3,
    borderColor: "#f4b400",
    borderStyle: "dotted",
  },
});
